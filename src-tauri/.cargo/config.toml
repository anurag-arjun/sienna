# Mold linker for faster linking on Linux.
# Install: sudo apt install mold clang (Ubuntu/Debian)
# Safe to ignore on macOS/Windows — this section only applies to the specified target.
[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=mold"]
